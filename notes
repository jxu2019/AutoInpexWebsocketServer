
sudo raspi-config

sudo apt-get update
sudo apt-get upgrade

sudo npm install pm2 -g
sudo npm install pm2@latest -g
pm2 update

sudo pip3 install cloudinary
sudo apt get install xscreensaver

sudo apt update
sudo apt install code -y
 sudo rpi-update

sudo apt install --reinstall libraspberrypi0 libraspberrypi-{bin,dev,doc} raspberrypi-bootloader raspberrypi-kernel

sudo pm2 start /home/pi/AutoInpexWebsocketServer/server.py --interpreter python3 -f 

sudo pm2 startup

sudo env PATH=$PATH:/usr/bin /usr/local/lib/node_modules/pm2/bin/pm2 startup systemd -u pi --hp /home/pi

sudo pm2 save

sudo pm2 stop all

sudo pm2 delete all

http://192.168.0.10:8000/ 
https://defgw.wordpress.com/2017/10/24/watchdog-reboot-raspberry-pi-if-network-is-down/


How to prevent screen blank

sudo apt-get install xscreensaver
Once installed, go to Rpi's desktop "Menu" (left top corner)
Go to preference --> screensaver.
You will see a screen saver main menu. In the mode drop-down menu, select "disable screensaver" then close the window.
Reboot the Raspberry PI.


sudo apt-get purge wolfram-engine
sudo apt-get purge libreoffice
sudo apt-get remove --purge libreoffice*
sudo apt-get clean && sudo apt-get autoremove --purge


sudo nano /boot/config.txt
over_voltage=6
force_turbo=1
disable_camera_led=1

sudo rm -rf AutoInpexWebsocketServer
git clone https://github.com/jxu2019/AutoInpexWebsocketServer.git


sudo pip3 install PyQt5

sudo dhclient -v
checking network connectin
ifconfig wlan0